name: 🚀 Deploy PHP BE via FTP to komer.id.vn

on:
  push:
    branches:
      - main   # Deploy tự động khi push lên nhánh main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Lấy source code
      - name: 🧭 Checkout source
        uses: actions/checkout@v4

      # 2️⃣ Upload toàn bộ mã nguồn PHP qua FTP
      - name: 📂 Upload via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          port: 21
          protocol: ftp     # hoặc ftps nếu host hỗ trợ SSL
          local-dir: ./     # thư mục chứa mã nguồn PHP (thường là root repo)
          server-dir: /domains/komer.id.vn/public_html/
          exclude: |
            **/.git*
            **/.github*
            **/node_modules/**
            **/README.md
            **/.env
